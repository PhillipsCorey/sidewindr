% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_tail.R
\name{slice.tail}
\alias{slice.tail}
\title{Pull Bottom Rows of a Dataframe}
\usage{
slice.tail(data, n)
}
\arguments{
\item{data}{a dataframe from which rows should be sliced.}

\item{n}{a double representing the number of rows to slice.}
}
\value{
The \code{data} dataframe, now sliced to only contain \code{n} rows per group in the dataframe.

If \code{n} is larger than the size of the total dataframe, it will return the entire dataframe.

If \code{n} is larger than a group when performing the operation groupwise, it will select the entire group.
}
\description{
\code{slice.tail} takes two parameters, a dataframe \code{data} and a number \code{n}. It will attempt to slice \code{n} rows out of the dataframe from the bottom of the dataframe and return these sliced rows as a new dataframe. If \code{data} is grouped, it will slice \code{n} rows for each group in \code{data}.

Note: Slicing a dataframe will remove the grouping indicies imposed by \code{group.by}. Please regroup dataframes following this operation.
}
\examples{
mtcars |> slice.tail(5)
# slice the last 5 rows of mtcars

mtcars |> group.by(cyl) |> slice.tail(2)
# slice the last 2 rows for each amount of cyl in the dataset

mtcars |> group.by(cyl) |> slice.tail(15)
# notice: 15 is larger than the size of our groups, so some group slices may include the entire group!
}
